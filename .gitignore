__pycache__/
*.pyc
*.pyo
.claude/settings.local.json

# ROS2 colcon build artifacts (install/ is a static copy, never symlinked)
build/
install/
log/

# Model weights — large binaries, download separately (see README)
model/

# Run logs / recorded plots / IDE workspace files — not needed to read or build the code
map_data/
plot_data/
timing/
plot/
*.code-workspace
